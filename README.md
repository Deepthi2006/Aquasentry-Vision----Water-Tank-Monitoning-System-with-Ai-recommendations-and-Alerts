venv\Scripts\activate   --- do activate

pip install fastapi uvicorn google-genai numpy pydantic python-multipart aiofiles groq


use this to run it
cd "d:\Aqua sentry live (important)";
uvicorn backend.main:app --reload --host 0.0.0.0 --port 5000

if :: 0:0:0:/5000 run localhost 5000


